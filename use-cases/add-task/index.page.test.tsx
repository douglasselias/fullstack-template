// import React from 'react'
// import { render, fireEvent, screen } from '@testing-library/react'

// import '@testing-library/jest-dom'

// import { IndexPageFactory } from '@/presentation/index-page-factory'

// test('Deve adicionar tarefa corretamente', () => {
//   render(<IndexPageFactory />)

//   const textInput = screen.getByPlaceholderText(/tarefa/i)
//   const submitButton = screen.getByText(/adicionar/i)

//   const description = 'description'
//   fireEvent.change(textInput, { target: { value: description } })
//   fireEvent.click(submitButton)

//   expect(screen.getByText(description)).toBeInTheDocument()
// })
export {}

test('hello', () => {
  expect(true).toBe(true)
})
